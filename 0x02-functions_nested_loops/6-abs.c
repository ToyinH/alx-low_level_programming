include "main.h"

/**
 * _abs - computes absolute value of integer
 * Return: absolute value 
 *
 */

int _abs(int)
{
	int n;

	if (n > 0)
		return (n);
	else if (n == 0)
		return (0);
	else
		return (n);
}
