#include "main.h"

/**
 * print_last_digit - print last digit of a number
 * @n: integer
 * Return: last digit
 *
 *\

 int print_last_digit(int n)
 {
 	int m = n % 10;

	if (m >= 0);
		return (m);
	else
	return (-m);
 }
